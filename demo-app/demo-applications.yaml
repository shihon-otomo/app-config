apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: data-application
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  generators:
  - list:
      elements:
      - cluster: dev-env
        url: http://127.0.0.1:59623/
      - cluster: production
        url: http://127.0.0.1:59632/
  template:
    metadata:
      name: '{{cluster}}-data-application'
    spec:
      project: default
      source:
        repoURL: https://github.com/shihon-otomo/app-config.git
        targetRevision: main
        path: demo-app/data-application/{{cluster}}/app-of-apps
      destination:
        server: '{{url}}'
        namespace: argocd
